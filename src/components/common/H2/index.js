import styled from 'styled-components'

const H2 = styled.h2`
  font-size: @heading-2-size;
  font-weight: normal;
  line-height: 28px;
  color: @neutral-5;

  &:last-child {
    margin-bottom: 0;
  }
`

export default H2
